//Task 0.7


function celsToFah(x) {
    x = x + 0;
    x = x * 9 / 5;
    x = x + 32;
    return x
} 

celsToFah(20)

function fahToCels(x) {
    x = x + 0;
    x = x - 32;
    x = x / 1.8
    return x
}

fahToCels(68)